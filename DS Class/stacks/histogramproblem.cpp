#include<bits/stdc++.h>
using namespace std;

#define int long long int

int32_t main(){
    int n;cin>>n;
    vector<int>a(n);
    for(int i = 0 ; i < n ; ++ i){
        cin>>a[i];
    }
    vector<int>rb(n);
    rb[n-1]=n;
    for(int i = n-2 ; i>=0 ; --i){
        
    }

    vector<int>lb(n);
    stack<int>l;
    ln[0]=-1;
    l.push(-1);
    for(int )

    max_area = 0;
    area = 0;
    for(int i = 0 ; i < n ; ++i ){
        area(rb[i]-lb[i]+1)*a[i];
    }
}